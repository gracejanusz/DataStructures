import java.util.Vector;

/*1. it sorts in reverse order.
 * 
 * 
 * 2. I would most likely create one comparator class that has an instance variable of the comparing class 
 * tracking the mode that is chosen. Then I would create a mutator method that changes the mode variable of 
 * the comparator. Then an if statement would evaluate which way to compare the date given!
 * 
 */



public class myVector<College> extends Vector<College>{

	public myVector() {
		super();
	}
	
}
